<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - ComicAura</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Spice&family=Lacquer&family=Pacifico&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/user/login.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/user/login.js" defer></script>
</head>
<body>
    <div class="login-container">
        <div class="row login-row">
            <!-- Left Side -->
            <div class="col-md-5 login-left">
                <div class="brand-logo">COMIC AURA</div>
                <h2>Welcome Back!</h2>
                <p>Login to access your favorite anime merchandise and exclusive deals.</p>
                
                <ul class="feature-list">
             <li><i class="fas fa-rocket"></i> Level Up Your Collection</li>
     
                    <li><i class="fas fa-tag"></i> Special discounts</li>
                    <li><i class="fas fa-box"></i> Unbox happienss</li>
                </ul>

            </div>

            <!-- Right Side -->
            <div class="col-md-7 login-right">
                <h2 class="mb-4">Login to Your Account</h2>

                <div class="social-login">
                    <button class="social-btn google" onclick="window.location.href='/auth/google'">
                        <i class="fab fa-google"></i>
                        Google
                    </button>
                    <button class="social-btn facebook">
                        <i class="fab fa-facebook-f"></i>
                        Facebook
                    </button>
                </div>

                <div class="divider">
                    <span class="px-3 bg-white">or</span>
                </div>

                <form id="loginForm" action="/login" method="POST">
                    <% if (typeof error !== 'undefined') { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error %>
                        </div>
                    <% } %>
                    <div class="form-floating mb-3">
                        <input type="email" class="form-control" id="email" placeholder="name@example.com" name="email">
                        <label for="email">Email address</label>
                    </div>

                    <div class="form-floating password-wrapper">
                        <input type="password" class="form-control" id="password" placeholder="Password" name="password">
                        <label for="password">Password</label>
                        <i class="fas fa-eye password-toggle"></i>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-3">
                   
                        <a href="/forgot-password" class="forgot-password">Forgot Password?</a>
                    </div>

                    <button type="submit" class="login-btn w-100">Login</button>

                    <div class="sign-up-link">
                        Don't have an account? <a href="/signup">Sign up</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>